- hosts: localhost
  vars:
     pwd: 
     username: "Mahesh"
  tasks:
        - name: install required softwar
          debug:
            msg: "Username is:- {{ username }} & Password is:- {{ pwd }}"



